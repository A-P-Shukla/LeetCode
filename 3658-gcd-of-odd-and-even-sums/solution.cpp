#include <numeric>

class Solution {
public:
    /**
     * Calculates the GCD of the sum of the first n odd numbers (n^2) 
     * and the sum of the first n even numbers (n^2 + n).
     * 
     * Using GCD(n^2, n^2 + n) = GCD(n^2, n) = n.
     */
    int gcdOfOddAndEvenSums(int n) {
        return n;
    }
};